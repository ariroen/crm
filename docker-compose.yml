version: "3.9"

services:
  bot:
    build: .
    container_name: contract61_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "${API_PORT:-8080}:${API_PORT:-8080}"
    environment:
      - TZ=${TZ:-Europe/Moscow}
